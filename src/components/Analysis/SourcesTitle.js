import React from 'react'

export default () => (
  <div className="sources-title-div">
    <div className="sources-title">Select your sources</div>
    <br />
    Note that while you can select any of the sources, they will only work if you have connected the corresponding app in the 'Account' tab.
  </div>
)
